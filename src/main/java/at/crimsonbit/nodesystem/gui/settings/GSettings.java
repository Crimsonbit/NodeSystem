package at.crimsonbit.nodesystem.gui.settings;

/**
 * <h1>GraphSettings</h1>
 * <p>
 * The GraphSettings enum defines every setting and or color that is manually
 * changeable in the nodesystem.
 * </p>
 * 
 * @author Florian Wagner
 * @verison 1.0
 */
public enum GSettings {

	SETTING_CURVE_WIDTH, SETTING_CURVE_CURVE, SETTING_SHADOW_WIDTH, SETTING_SHADOW_HEIGHT, SETTING_SHADOW_RADIUS;
}
